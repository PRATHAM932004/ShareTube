import authSlice from './authSlice';
import likeSlice from './likeSlice';
import videoSlice from './videoSlice';
import userSlice from './userSlice';

export { authSlice, videoSlice, likeSlice, userSlice };
